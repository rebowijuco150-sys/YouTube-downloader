"""
فایل تنظیمات ربات یوتیوب دانلودر
"""

import os
from dotenv import load_dotenv

# بارگذاری متغیرهای محیط
load_dotenv()

# ===================== تنظیمات تلگرام =====================
TELEGRAM_TOKEN = os.getenv('TELEGRAM_TOKEN', '')
if not TELEGRAM_TOKEN:
    raise ValueError("⚠️ TELEGRAM_TOKEN در فایل .env تعریف نشده است!")

# ===================== تنظیمات دانلود =====================
DOWNLOAD_PATH = os.getenv('DOWNLOAD_PATH', './downloads')
MAX_FILE_SIZE = int(os.getenv('MAX_FILE_SIZE', 2000))  # MB
DOWNLOAD_TIMEOUT = int(os.getenv('DOWNLOAD_TIMEOUT', 300))  # ثانیه
MAX_CONCURRENT_DOWNLOADS = int(os.getenv('MAX_CONCURRENT_DOWNLOADS', 3))

# ایجاد پوشه دانلود اگر وجود نداشته باشد
os.makedirs(DOWNLOAD_PATH, exist_ok=True)

# ===================== تنظیمات Logging =====================
LOG_LEVEL = os.getenv('LOG_LEVEL', 'INFO')

# ===================== کیفیت‌های دستیاب =====================
QUALITIES = {
    '1080p': '1080',
    '720p': '720',
    '480p': '480',
    '360p': '360',
    '240p': '240',
    '144p': '144'
}

# ===================== پیام‌های ربات =====================
MESSAGES = {
    'welcome': """
🎥 **خوش آمدید به ربات دانلود یوتیوب!**

من می‌تونم کمکتون کنم برای:
✅ دانلود ویدیو‌های یوتیوب
✅ دانلود فقط صدا (آهنگ)
✅ دانلود لیست پخش‌ها

برای شروع یک لینک یوتیوب ارسال کنید یا از دستورات استفاده کنید.

📝 دستورات:
/help - نمایش راهنمایی
/download - دانلود ویدیو
/audio - دانلود صدا
/status - وضعیت دانلود
    """,

    'help': """
📖 **راهنمایی استفاده:**

1️⃣ **دانلود ویدیو:**
   - لینک یوتیوب را ارسال کنید
   - کیفیت دلخواه را انتخاب کنید
   - منتظر دانلود باشید

2️⃣ **دانلود صدا:**
   - /audio <لینک> را ارسال کنید
   - فقط صدای ویدیو دانلود می‌شود

3️⃣ **دانلود لیست پخش:**
   - لینک لیست پخش را ارسال کنید
   - تمام ویدیو‌ها دانلود می‌شوند

⚠️ **محدودیت‌ها:**
- حداکثر اندازه فایل: {} MB
- تایم‌اوت دانلود: {} ثانیه

👨‍💻 برای کمک بیشتر: /help
    """.format(MAX_FILE_SIZE, DOWNLOAD_TIMEOUT),

    'invalid_url': '❌ لینک نامعتبر است! لطفاً یک لینک یوتیوب معتبر ارسال کنید.',
    'downloading': '⏳ در حال دانلود... لطفاً منتظر باشید.',
    'processing': '🔄 در حال پردازش... این ممکن است چند لحظه طول بکشد.',
    'success': '✅ دانلود با موفقیت انجام شد!',
    'error': '❌ خطایی رخ داد! لطفاً دوباره تلاش کنید.',
    'file_too_large': '📦 فایل بیش از حد بزرگ است! حداکثر {}MB'.format(MAX_FILE_SIZE),
    'select_quality': '📊 لطفاً کیفیت دلخواه را انتخاب کنید:',
}

# ===================== فرمت‌های فایل =====================
AUDIO_FORMAT = 'mp3'
VIDEO_FORMAT = 'mp4'

# ===================== بیلاکلیست =====================
BLOCKED_DOMAINS = []  # اگر لازم باشد دامنه‌های خاصی را مسدود کنید

print("✅ تنظیمات بارگذاری شدند")
